# .uicraft.md

## Design Context

### Users

- **知识工作者 / 研究员**：时间有限，需要一个议题在几分钟内被多角度拆解，产出可引用的争辩与摘要。
- **产品 / 决策人**：方案评审前想先看到各立场与潜在争议，把 AI 圆桌当作"预演沙盘"。
- **教育与内容创作者**：需要把复杂议题结构化呈现给受众，追求"可录制、可回放"的虚拟专家访谈素材。

使用场景：桌面浏览器为主，长时间注视（一场讨论 5–15 分钟），中文界面，需要"正在直播"的临场感。

### Brand Personality

- 三个词：**权威、临场、克制**。
- 情绪目标：像坐在一间深夜的电视演播厅控制室里看一场高端圆桌——专业、有分量、信息密度高但不吵。
- 语气：中文，短句，主持人式旁白（"我们进入第二轮，先请……"），不使用网络梗与感叹号堆砌。

### Aesthetic Direction

- **主题**：深色单主题（演播厅暗场）。不用浅色切换，避免"通用 SaaS 后台"观感。
- **色彩主张**：以近黑墨色为底（`#0B0C0F` 级），主强调色选用**舞台琥珀金**（`#E7B24A` 一族）而非默认蓝色 SaaS 蓝；"ON AIR" 使用低饱和朱红。每位嘉宾分配一个**独立高辨识度色相**，用于 transcript 色块、状态灯与共识/分歧标签，颜色即身份。
- **排版**：标题与议题使用**衬线体**（编辑部/新闻质感，中文回退宋体系），UI 与正文使用无衬线（中文回退黑体系），时间戳与状态用等宽体。字号层级用 3:1 以上跨度，靠字重+颜色+留白叠加建立层次，而非只靠放大。
- **反参考**：通用蓝色 SaaS 仪表盘、无差别卡片网格、无处不在的毛玻璃与装饰性光晕、渐变文字。

### Design Principles

1. **颜色即身份**：嘉宾的颜色必须在状态灯、发言块、共识/分歧标签三处保持一致，用户靠颜色而不是靠读名字来追踪谁在说话。
2. **直播感来自节奏而非特效**：用逐字/逐条入场、状态灯呼吸、ON AIR 指示、时间轴推进来传达"正在发生"，动效时长收敛在 100–500ms，只动 `transform` / `opacity`。
3. **信息密度优先，装饰让位**：演播厅是三栏高密度信息容器，先解决层级、可读性与独立滚动，再谈氛围。
4. **滚动边界即容器边界**：整页不滚动；每个区域（嘉宾席、transcript、共识板）在自己的容器内独立滚动，超宽屏/常规屏/窄屏都给出合理的区域分配。
5. **降级必须可用**：`prefers-reduced-motion` 下保留信息性动效但去掉位移；入场动画必须 fail-open（JS 未执行时内容依然可见）；未配置 API Key 时讨论依然能完整跑完。
